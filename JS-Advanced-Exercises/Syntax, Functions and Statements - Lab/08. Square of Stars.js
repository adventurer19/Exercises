function main(input){
    let row = '';
    for (let i = 0; i < input; i++) {
        for (let j = 0; j < input; j++) {
         row +=`${'* '}`
                
        }
        console.log(row)
        row='';
    }
}